{
	"name": "mcclowes-react-scripts",
	"description": "Work in progress",
	"version": "3.0.3",
	"author": "Max Clayton Clowes <maxcc@me.com>",
	"repository": "https://bitbucket.org/mcclowes/mcclowes-react-scripts",
	"license": "MIT",
	"eslintConfig": {
		"extends": "mcclowes"
	},
	"scripts": {
		"publish": "lerna run format && lerna run build && lerna publish --force-publish=* from-git"
	},
	"babel": {
		"presets": [
			"mcclowes"
		]
	},
	"devDependencies": {},
	"dependencies": {
		"babel-core": "^6.26.0",
		"css-loader": "^1.0.0",
		"eslint": "^5.6.0",
		"graphql": "^14.0.2",
		"lerna": "^3.4.0",
		"svg-inline-loader": "^0.8.0"
	}
}
